'use client'; import { useState } from 'react'; export default function Dashboard(){ const [tab,setTab]=useState('generate'); return (<div className='bg-white p-6 rounded-lg shadow max-w-4xl mx-auto'><div className='flex items-center justify-between'><h2 className='text-2xl font-semibold'>Criar Conteúdo</h2><div className='flex gap-2'><button onClick={()=>setTab('generate')} className={`px-3 py-1 rounded ${tab==='generate'? 'bg-violet-600 text-white' : 'border'}`}>Gerar</button><button onClick={()=>setTab('history')} className={`px-3 py-1 rounded ${tab==='history'? 'bg-violet-600 text-white' : 'border'}`}>Histórico</button></div></div><div className='mt-6'>{tab==='generate'? (<div>Formulário de geração (envia para /api/generate-text)</div>) : (<div>Histórico de anúncios (chama /api/history)</div>)}</div></div>); }
